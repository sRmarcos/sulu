{% block resource_locator_widget %}
<div id="{{ id|raw }}"
     class="preview-update trigger-save-button"
     data-property='{{ property|json_encode }}' 
     data-mapper-property="{{ name }}"
     data-form="true"
     data-type="resourceLocator"
     data-aura-component="resource-locator@sulucontent"
     data-type-instance-name="{{ id }}"
     data-aura-history-api="/admin/api/nodes/<%=content.id%>/resourcelocators?language={{ locale }}&webspace={{ webspace_key }}"
     data-aura-content-id="<%=options.id%>"
     {% if property.mandatory %}data-validation-required="true"{% endif %} ></div>
{% endblock %}

{% block text_area_widget %}
<textarea id="{{ id|raw }}"
          data-property='{{ property|json_encode }}'
          class="form-element preview-update trigger-save-button hide-in-sortmode"
          data-mapper-property="{{ property.name }}"
          placeholder="{{ property.getPlaceholder(userLocale) }}"
          {% if property.mandatory %}data-validation-required="true"{% endif %}></textarea>

<div class="block-content" data-sort-mode-id="{{ id|raw }}"></div>
{% endblock %}
